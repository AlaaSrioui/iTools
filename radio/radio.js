function play(audio) {     
    document.getElementById(audio).play()  // function that takes a source audio as a parameter and play it 
  }

  function pause(audio) {
    document.getElementById(audio).pause() // function that takes a source audio as a parameter and pause it 
  }